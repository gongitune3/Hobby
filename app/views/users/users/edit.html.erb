    <div class="row">
        <div class="col-xs-7 offset-md-1">
            <h3>登録情報編集</h3>
            <%= render '/layouts/errors', obj: @user %>
        </div>
    </div>
        <div class="row">
            <div class="col-xs-9 offset-md-1">
                <%= form_with model: @user, url: users_user_path(@user), local: :true, method: :patch do |f| %>
                    <div class="form-group">
                    <div class="field">
                        <%= f.label :ハンドルネーム, class: "mt-4" %><br />
                        <%= f.text_field :nickname, class: "form-control" %>
                    </div>
                    <div class="field">
                        <%= f.label :自己紹介, class: "mt-4" %><br />
                        <%= f.text_area :introduction, class: "form-control" %>
                    </div>
                    <div class="field">
                        <%= f.label :プロフ画像, class: "mt-4" %><br />
                        <%= f.attachment_field :profile_image %>
                    </div>
                        <%= f.submit "編集内容を保存する", class: "btn btn-secondary mt-4" %>
                    </div>
                <% end %>
            <%# <div class="delete">
                <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modal1">
                    退会
                </button>
                        <div class="modal fade" id="modal1" tabindex="-1"
                            role="dialog" aria-labelledby="label1" aria-hidden="true">
                        <div class="modal-dialog" role="document">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="label1">ユーザー情報を消しますか？</h5>
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                </div>
                                <div class="modal-body">
                                    ユーザーを削除しますと、復元ができません。<br>
                                    宜しければ下記の削除のボタンをクリックしてください。
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">キャンセル</button>
                                    <%= link_to users_board_path(@board.id), method: :delete, remote: true do %>
                                        <%# <button type="button" class="btn btn-danger">削除</button>
                                    <% end %>
                                <%# </div>
                            </div>
                        </div>
                        </div>
            </div> %> 
        </div>
    </div>
