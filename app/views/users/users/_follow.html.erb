<% if current_user.following?(user) %>
    <%= link_to 'フォロー外す', users_user_relationships_path(user.id), method: :delete, remote: true, class: "btn btn-primary" %>
<% else %>
    <%= link_to 'フォローする', users_user_relationships_path(user.id), method: :POST, remote: true, class: "btn btn-success" %>
<% end %>